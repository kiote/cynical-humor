<h2><%= @post.title %></h2>
<div>
  <span class="author"><%= @post.author.name %></span>
  <span class="published_on"><%= @post.published_on.strftime "%b %d, %Y" %></span>
</div>
<div><%= @post.body %> </div>